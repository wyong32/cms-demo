# =============================================================================
# cms-demo 前端（Vite）环境变量模板
# 使用方式：复制为 .env 或 .env.local（建议加入 .gitignore，勿提交密钥）
# =============================================================================

# -----------------------------------------------------------------------------
# 后端 API 根地址（见 src/utils/apiBase.js）
# -----------------------------------------------------------------------------
# 不设置时：
#   - 开发模式 (npm run dev)：请求前缀为 /api，由 vite.config 代理到 target
#   - 生产构建且非 localhost：默认直连 https://cms-demo-api.vercel.app/api
# 若前后端分离部署，在此写你的 API 根地址（是否带 /api 需与部署一致，一般与 getApiBaseURL 约定一致）
# 示例（按需二选一）：
# VITE_API_URL=https://your-api.example.com/api
# VITE_API_URL=http://127.0.0.1:3001/api
